import React from 'react';
import NavBar from './NavBar';  // Ensure the correct casing of the file name

const StudentDashboard = () => {
  return (
    <div>
      <NavBar />
      <h1>Welcome to the Student Dashboard</h1>
    </div>
  );
};

export default StudentDashboard;
